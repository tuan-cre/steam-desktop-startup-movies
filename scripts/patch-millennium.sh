#!/usr/bin/env bash
# DEPRECATED (kept as a stub so old references don't break).
#
# Recent Steam clients launch steamwebhelper with --autoplay-policy stock, so
# the plugin's unmuted autoplay works without patching Millennium. The muted-
# first hybrid fallback remains for environments without the flag.
#
# Full script preserved in git history if ever needed again.
set -euo pipefail

echo "DEPRECATED: Steam now ships --autoplay-policy stock - no patch needed."
echo "Nothing to do. (See git history of this file for the old patch.)"
exit 0
